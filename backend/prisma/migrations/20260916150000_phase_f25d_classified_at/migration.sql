-- F25-D: แยก "ค่าเริ่มต้นของระบบ" ออกจาก "การตัดสินใจของคน"
--
-- คอลัมน์ classification ที่เพิ่มไปก่อนหน้านี้ตั้งค่าเริ่มต้นให้ทุกแถวเป็น INTERNAL
-- ซึ่งเป็นค่าที่อนุรักษ์นิยมแต่ไม่มีใครเลือก ถ้าไม่แยกสองกรณีนี้ รายงานการจัดชั้นความลับ
-- จะรายงานว่าทั้งระบบ "จัดชั้นแล้ว" ทั้งที่ยังไม่มีใครดูเลยแม้แต่ฉบับเดียว
--
-- NULL = ยังไม่เคยมีใครกำหนด ใช้ค่าเริ่มต้นของระบบอยู่
-- มีค่า = มีคนกำหนดเมื่อเวลานั้น (ใคร/จากอะไรเป็นอะไร/เพราะอะไร อยู่ใน activity_logs)
--
-- เพิ่มอย่างเดียว ไม่แก้ ไม่ลบข้อมูลเดิม แถวที่มีอยู่ทั้งหมดได้ NULL ซึ่งตรงกับความจริง
ALTER TABLE `resources` ADD COLUMN `classifiedAt` DATETIME(3) NULL;
